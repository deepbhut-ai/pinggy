# callingagents.in fleet — HTTPS for 33 CodeCanyon app subdomains
# Managed by Coading v2.6.0 — DO NOT EDIT MANUALLY (use ssl_manager or this file)
#
# Architecture:
#   - Default 443 server: self-signed wildcard for *.callingagents.in (fallback)
#   - Per-subdomain 443 servers: real LE certs override the default (added as DNS records get created)
#   - iraglobaltech.com 443: existing LE cert for the main app domain
#   - Port 80: ACME challenge + HTTP→HTTPS redirect (in iragt.react.conf)

# ---- HTTPS for iraglobaltech.com (main app domain) ----
server {rewrite ^(/.well-known/acme-challenge/.*) $1 break; # managed by Certbot


    listen 443 ssl;
    listen [::]:443 ssl;
    server_name iraglobaltech.com;

    ssl_certificate     /etc/letsencrypt/live/iraglobaltech.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/iraglobaltech.com/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;

    add_header Strict-Transport-Security "max-age=31536000" always;

    # ACME challenge for cert renewal
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # React SPA
    root /opt/iragt/dist;
    index index.html;

    limit_req zone=per_ip_rate burst=1000 nodelay;
    limit_conn per_ip_conn 100;
    limit_req_status 429;
    limit_conn_status 429;

    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }

    location = /health {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
    }
    location /admin {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    location /docs {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
    }
    location /redoc {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
    }

    location / {
        try_files $uri /index.html;
    }

    access_log /var/log/nginx/iragt_ssl_access.log;
    error_log /var/log/nginx/iragt_ssl_error.log;
location = /.well-known/acme-challenge/ihqmQbTnhqOGukmOvchO5RmgA1Ynue8oGzAg96Np6Dg{default_type text/plain;return 200 ihqmQbTnhqOGukmOvchO5RmgA1Ynue8oGzAg96Np6Dg.DP1c-f6-EPL5WEvHtsnK_9jYZaAfqjSgZyVz9wUu0aY;} # managed by Certbot

}

# ---- HTTPS for webifly.callingagents.in (real LE cert — first fleet sub) ----
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name webifly.callingagents.in;

    ssl_certificate     /etc/letsencrypt/live/webifly.callingagents.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/webifly.callingagents.in/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;

    add_header Strict-Transport-Security "max-age=31536000" always;

    # ACME challenge for cert renewal
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # WebSocket routes — long timeout, no buffering (v2.11.0)
    location /ws/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_connect_timeout 30s;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        proxy_buffering off;
    }

    # Proxy to FastAPI tunnel proxy
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_connect_timeout 30s;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
        proxy_buffering off;
    }

    access_log /var/log/nginx/fleet_ssl_access.log;
    error_log /var/log/nginx/fleet_ssl_error.log;
}

# ---- HTTPS default for *.callingagents.in + custom domains (self-signed fallback) ----
# When a sub gets a real LE cert, add a dedicated server block above (like webifly).
# This default catches all other subs so 443 answers instead of refusing.
server {
    listen 443 ssl default_server;
    listen [::]:443 ssl default_server;
    server_name _;

    # Self-signed wildcard — replaced per-subdomain by LE certs above
    ssl_certificate     /etc/letsencrypt/live/callingagents.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/callingagents.in/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;

    # ACME challenge for new cert issuance
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Health check
    location = /health {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # WebSocket routes — long timeout, no buffering (v2.11.0)
    location /ws/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_connect_timeout 30s;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        proxy_buffering off;
    }

    # Proxy all tunnel traffic to FastAPI
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_connect_timeout 30s;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
        proxy_buffering off;
    }

    access_log /var/log/nginx/fleet_default_ssl_access.log;
    error_log /var/log/nginx/fleet_default_ssl_error.log;
}