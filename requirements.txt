# FastAPI
fastapi==0.115.0
uvicorn[standard]==0.30.6

# PostgreSQL — psycopg3 (sync + async in one package)
psycopg[binary,pool]==3.2.1

# Settings / env
pydantic[email]==2.8.2
pydantic-settings==2.4.0

# Auth
bcrypt==4.2.0
PyJWT==2.9.0

# Migrations
alembic==1.13.2