# Bytecode / caches
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Env
.env
.env.production

# SSH host keys (auto-generated)
ssh_host_key
ssh_host_key.pub

# IDE
.vscode/
.idea/

# Frontend (React/Vite) — node_modules is huge, dist is a build artifact
node_modules/
frontend/dist/
frontend/node_modules/

# OS
Thumbs.db
.DS_Store

# Database backups (contain sensitive data — never commit)
backups/backups/
